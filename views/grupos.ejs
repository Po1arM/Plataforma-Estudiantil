<%- include('partials/_header1') %>

<div class = "row">
    <div class="col-xl-4 ">
        <h1>Grupos</h1>
    </div>
    <div class="col-xl-4 ">
            <button onclick = "location.href='/crearGrupo'"  type="button" class="btn btn-secondary btn-min-width mr-1 mb-1">Añadir</button>
    </div>
    <div class="col-xl-4 ">
            <button onclick = "myFunction()"  type="button" class="btn btn-secondary btn-min-width mr-1 mb-1">Generar con Pensums
            </button>
    </div>
        </div>
    </div>
</div>
<div class="table-responsive">
    <table  class="table table-bordered bg-white">
        <thead class="thead-dark">
            <th>Codigo</th>
            <th>Curso</th>
            <th>Nivel</th>
            <th>Materia</th>
            <th>Horario</th>
            <th>Maestro</th>
            <th>Modificar</th>
            <th>Eliminar</th>
        </thead>
        <tbody>
            <% for(var i = 0; i < grupos.length; i++) { %>
                <tr>
                    <td> <%= grupos[i].codigo %> </td>
                    <td> <%= grupos[i].curso %> </td>
                    <td> <%= grupos[i].nivel %> </td>
                    <td> <%= grupos[i].materia %> </td>
                    <td> <%= grupos[i].horario %> </td>
                    <td> <%= grupos[i].maestro %> </td>
                    <td><a href="/modificarGrupo/<%= grupos[i]._id%>"><i class="fa fa-pencil" ></i></a></td>
                    <td><a href="/eliminarGrupo/<%= grupos[i]._id%>"><i class="fa fa-trash" ></i></a></td>
                </tr>
            <% }%>  
        </tbody>
                    
    </table>
    <script>
        function myFunction() {
          var txt;
          if (confirm("Si genera a partir del pensum, todos los grupos se crearan automaticamente y los grupos actuales serán eliminados!")) {
            window.location.href = '/generarPensum'
        } else {
            window.location.href = '/grupos'
          }
          document.getElementById("demo").innerHTML = txt;
        }
        </script>
      <!-- BEGIN VENDOR JS-->
      <script src="vendors/js/vendors.min.js" type="text/javascript"></script>
      <!-- BEGIN VENDOR JS-->
      <!-- BEGIN PAGE VENDOR JS-->
      <!-- END PAGE VENDOR JS-->
      <!-- BEGIN CHAMELEON  JS-->
      <script src="js/core/app-menu-lite.js" type="text/javascript"></script>
      <script src="js/core/app-lite.js" type="text/javascript"></script>
      <!-- END CHAMELEON  JS-->
      <!-- BEGIN PAGE LEVEL JS-->
      <!-- END PAGE LEVEL JS-->
    </body>
  </html>